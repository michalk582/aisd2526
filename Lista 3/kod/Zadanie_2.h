#ifndef ZADANIE1_H
#define ZADANIE1_H

#include <vector>

// Tutaj tylko DEKLARUJEMY funkcje (obiecujemy, że istnieją)
int CUT_ROD_NAIVE(const std::vector<int>& p, int n);
int MEMOIZED_CUT_ROD(const std::vector<int>& p, int n);
int BOTTOM_UP_CUT_ROD(const std::vector<int>& p, int n);

#endif //ZADANIE1_H